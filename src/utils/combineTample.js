module.exports = function combineTample(strings,vals){
  const result = [strings[0]];
   vals.forEach( val => {
     result.push(val, strings[i + 1]);
   });
   return result.join("");
} // END combine
